generator client {
  provider = "prisma-client"
  output   = "../src/prisma-generated"
}

datasource db {
  provider = "mysql"
}

model TestTable {
  id    Int    @id @default(autoincrement())
  value String

  @@map("test_table")
}
